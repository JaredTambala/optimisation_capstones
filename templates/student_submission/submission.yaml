capstone_id: "CAP-001"
capstone_version: "0.3.0"
data_version: "0.3.2"
student:
  identifier: "ABC123"
technology:
  language: "python"
  application_framework: "streamlit"
  optimisation_libraries:
    - "pyomo"
commands:
  setup: "python -m pip install -e ."
  test: "pytest"
  reproduce_reference: "python -m src.run --mode reproduce-reference --config config/default.yaml"
  solve_default: "python -m src.run --model recursive --config config/default.yaml"
  run_app: "streamlit run app/app.py"
reproducibility:
  random_seeds:
    - 42
    - 314
    - 2718
outputs:
  evaluation_directory: "artifacts/evaluation"
